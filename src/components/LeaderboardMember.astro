---
interface Props {
  member: MemberWithId;
}

const { member } = Astro.props;

import { getDollarsPerDev, fmtCurrency } from '../members.ts';
import type { MemberWithId } from "../content/config.ts";
---

<td>
  <a class="sneaky" href={`/members/${member.id}`}>
    <img src={member.data.urlSquareLogoWithBackground}>
    {member.data.name}
  </div>
</td>
<td>
  {fmtCurrency(getDollarsPerDev(member.data.annualReports[0]))}
</td>

<style>
  img {
    max-width: 3.5rem;
    margin-right: 1rem;
  }
  a {
    display: flex;
    align-items: center;
    min-width: 15rem;
    font-weight: bold;
  }
</style>
